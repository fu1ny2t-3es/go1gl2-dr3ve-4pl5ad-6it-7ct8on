on:
  workflow_dispatch:
  schedule:
    - cron: '45 1 */6 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/go1gl2-dr3ve-4pl5ad-6it-7ct8on@cron
        with:
          token: ${{ secrets.token }}
